{
  "name": "03 - Memory & Context Builder",
  "nodes": [
    {
      "parameters": {
        "workflowInputs": {
          "values": [
            {
              "name": "conversation_id"
            },
            {
              "name": "contact_id"
            },
            {
              "name": "phone"
            },
            {
              "name": "chat_id"
            },
            {
              "name": "business_id"
            },
            {
              "name": "customer_name"
            },
            {
              "name": "message"
            },
            {
              "name": "message_id"
            },
            {
              "name": "hasMedia",
              "type": "boolean"
            },
            {
              "name": "timestamp"
            },
            {
              "name": "request_id"
            }
          ]
        }
      },
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "typeVersion": 1.2,
      "position": [
        -416,
        -96
      ],
      "id": "109dbfca-2a58-47d2-9bc5-d7b838251141",
      "name": "When Executed by Another Workflow"
    },
    {
      "alwaysOutputData": true,
      "id": "get-business-by-phone",
      "name": "Get Business by Phone",
      "type": "n8n-nodes-base.postgres",
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.7,
      "parameters": {
        "query": "SELECT id, name, slug, currency, timezone FROM businesses WHERE id = $1::uuid AND status = 'active' AND deleted_at IS NULL LIMIT 1;",
        "operation": "executeQuery",
        "options": {
          "continueOnFail": true,
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        }
      },
      "position": [
        -176,
        -200
      ]
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id, business_id, phone, display_name, source, created_at, updated_at FROM contacts WHERE id = $1::uuid AND business_id = $2::uuid;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.contact_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        -176,
        -48
      ],
      "id": "8e63a3fe-e73e-40bd-a63c-02d388ff6892",
      "name": "Get Contact",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT id, business_id, contact_id, channel, status, last_message_at, created_at, updated_at FROM conversations WHERE id = $1::uuid AND business_id = $2::uuid;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.conversation_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        -32,
        -64
      ],
      "id": "35da178e-c1c5-4a83-92be-536ee2634263",
      "name": "Get Conversation",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n    json_agg(\n        json_build_object(\n            'direction', direction,\n            'sender_type', sender_type,\n            'text_body', text_body,\n            'created_at', created_at\n        )\n        ORDER BY created_at ASC\n    ),\n    '[]'::json\n) AS messages\nFROM (\n    SELECT direction, sender_type, text_body, created_at\n    FROM messages m\n    WHERE m.conversation_id = $1::uuid\n      AND EXISTS (SELECT 1 FROM conversations c WHERE c.id = m.conversation_id AND c.business_id = $2::uuid)\n    ORDER BY created_at DESC\n    LIMIT 10\n) recent;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.conversation_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        128,
        0
      ],
      "id": "3f2bd6b2-8a7d-4136-89f2-3ac2d0f1c21e",
      "name": "Get Recent Messages",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'fact_key', cf.fact_key,\n      'fact_value', cf.fact_value,\n      'confidence', cf.confidence\n    )\n    ORDER BY cf.confidence DESC, cf.updated_at DESC\n  ),\n  '[]'::json\n) AS facts\nFROM customer_facts cf\nJOIN contacts c ON c.id = cf.contact_id\nWHERE cf.contact_id = $1::uuid AND c.business_id = $2::uuid;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.contact_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        304,
        -48
      ],
      "id": "7816afa7-1175-46a3-a6fb-54bf8be2e5a3",
      "name": "Retrieve Customer Facts",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(summary, '') AS summary, COALESCE(message_count, 0) AS message_count, COALESCE(created_by, 'none') AS created_by\nFROM conversation_summaries\nWHERE conversation_id = $1::uuid\nLIMIT 1;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.conversation_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        464,
        32
      ],
      "id": "5787d93c-4fa9-4775-b6fb-4f46efbf9e25",
      "name": "Get Conversation Summary",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://router.huggingface.co/hf-inference/models/BAAI/bge-base-en-v1.5",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "Bearer {{ $env.HUGGINGFACE_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { inputs: \"Represent this sentence for searching relevant passages: \" + String($('When Executed by Another Workflow').first().json.message || ''), options: { wait_for_model: true } } }}",
        "options": {
          "timeout": 30000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.4,
      "position": [
        464,
        160
      ],
      "id": "generate-embedding-node-id",
      "name": "Generate Message Embedding",
      "alwaysOutputData": true,
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const embeddingResult = $('Generate Message Embedding').first()?.json;\nconst message = $('When Executed by Another Workflow').first()?.json?.message || '';\n\nlet embedding = null;\nif (embeddingResult && Array.isArray(embeddingResult) && embeddingResult.length > 0) {\n  embedding = embeddingResult[0];\n} else if (embeddingResult && embeddingResult[0] && Array.isArray(embeddingResult[0])) {\n  embedding = embeddingResult[0][0];\n}\n\nconst hasValid = Boolean(embedding && Array.isArray(embedding) && embedding.length === 768);\nconst dummyUnitVector = [1, ...Array(767).fill(0)];\nconst vectorStr = hasValid ? '[' + embedding.join(',') + ']' : '[' + dummyUnitVector.join(',') + ']';\n\nreturn [{\n  json: {\n    embedding_vector: vectorStr,\n    message_text: message,\n    has_vector: hasValid\n  }\n}];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        608,
        100
      ],
      "id": "prepare-search-params-id",
      "name": "Prepare Search Params",
      "alwaysOutputData": true
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'chunk_text', chunk_text,\n      'metadata', metadata,\n      'source', source,\n      'similarity', similarity\n    )\n    ORDER BY similarity DESC\n  ),\n  '[]'::json\n) AS knowledge\nFROM (\n  SELECT kc.chunk_text, kc.metadata, kd.title AS source, 1 - (kc.embedding <=> $1::vector) AS similarity\n  FROM knowledge_chunks kc\n  JOIN knowledge_documents kd ON kd.id = kc.document_id\n  WHERE kd.status = 'indexed'\n    AND kd.business_id = $4::uuid\n    AND kc.embedding IS NOT NULL\n    AND $3::boolean = true\n    AND kc.chunk_text NOT ILIKE 'dummy content%'\n    AND 1 - (kc.embedding <=> $1::vector) > 0.35\n  UNION ALL\n  SELECT kc.chunk_text, kc.metadata, kd.title AS source, COALESCE(similarity(kc.chunk_text, $2), 0) AS similarity\n  FROM knowledge_chunks kc\n  JOIN knowledge_documents kd ON kd.id = kc.document_id\n  WHERE kd.status = 'indexed'\n    AND kd.business_id = $4::uuid\n    AND similarity(kc.chunk_text, $2) > 0.1\n    AND kc.chunk_text NOT ILIKE 'dummy content%'\n  ORDER BY similarity DESC\n  LIMIT 10\n) combined;",
        "options": {
          "queryReplacement": "={{ $('Prepare Search Params').first().json.embedding_vector }}, {{ $('Prepare Search Params').first().json.message_text }}, {{ $('Prepare Search Params').first().json.has_vector }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        }
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        608,
        -48
      ],
      "id": "f3a4b5c6-d7e8-4901-9a2b-3c4d5e6f7a8b",
      "name": "Get Knowledge Chunks (semantic)",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const items = $input.all();\nlet knowledge = [];\nconst firstItem = items[0]?.json || {};\n\nif (firstItem.knowledge && Array.isArray(firstItem.knowledge)) {\n  knowledge = firstItem.knowledge;\n} else if (firstItem.success && Array.isArray(firstItem.data)) {\n  knowledge = firstItem.data.map(hit => {\n    const chunk_text = hit.chunk_text || '';\n    const metadata = hit.metadata || {};\n    return {\n      chunk_text,\n      metadata,\n      source: typeof metadata === 'string' ? metadata : (hit.source || metadata.title || metadata.document_title || '')\n    };\n  }).filter(k => !!k.chunk_text);\n} else if (firstItem.result && Array.isArray(firstItem.result)) {\n  knowledge = firstItem.result.map(hit => {\n    const payload = hit.payload || {};\n    const chunk_text = payload.chunk_text || payload.pageContent || '';\n    const metadata = payload.metadata || payload || {};\n    return {\n      chunk_text,\n      metadata,\n      source: typeof metadata === 'string' ? metadata : (metadata.title || metadata.document_title || '')\n    };\n  }).filter(k => !!k.chunk_text);\n} else {\n  knowledge = items\n    .map(item => {\n      const doc = item.json.document || item.json || {};\n      const chunk_text = doc.pageContent || doc.chunk_text || item.json.chunk_text || '';\n      if (!chunk_text) return null;\n      const metadata = doc.metadata || item.json.metadata || {};\n      return {\n        chunk_text,\n        metadata,\n        source: typeof metadata === 'string' ? metadata : (metadata.title || metadata.document_title || ''),\n        score: item.json.score ?? null\n      };\n    })\n    .filter(Boolean);\n}\n\nreturn [{ json: { knowledge } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        608,
        100
      ],
      "id": "b9a4b8d7-f661-48ad-b97e-154cc4577af0",
      "name": "Get Knowledge Chunks"
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'id', id,\n      'sku', sku,\n      'name', name,\n      'description', description,\n      'price', price,\n      'currency', currency,\n      'unit', unit,\n      'category', category,\n      'product_type', product_type\n    )\n    ORDER BY name\n  ),\n  '[]'::json\n) AS products\nFROM products \nWHERE active = true AND business_id = $1::uuid\nLIMIT 50;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        768,
        -48
      ],
      "id": "d8bab17d-bcb1-4d99-95b9-70b9191cab8b",
      "name": "Get Products",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'id', id,\n      'quote_number', quote_number,\n      'status', status,\n      'total', total,\n      'currency', currency,\n      'valid_until', valid_until\n    )\n    ORDER BY created_at DESC\n  ),\n  '[]'::json\n) AS quotes\nFROM quotes\nWHERE contact_id = $1::uuid AND business_id = $2::uuid\nLIMIT 20;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.contact_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        928,
        -48
      ],
      "id": "get-quotes-node-id",
      "name": "Get Quotes",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'id', id,\n      'title', title,\n      'status', status,\n      'starts_at', starts_at,\n      'ends_at', ends_at\n    )\n    ORDER BY starts_at DESC\n  ),\n  '[]'::json\n) AS appointments\nFROM appointments\nWHERE contact_id = $1::uuid AND business_id = $2::uuid\nLIMIT 20;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.contact_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        1088,
        -48
      ],
      "id": "get-appointments-node-id",
      "name": "Get Appointments",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "operation": "executeQuery",
        "query": "SELECT COALESCE(\n  json_agg(\n    json_build_object(\n      'id', id,\n      'status', status,\n      'reason', reason,\n      'assigned_to', assigned_to,\n      'created_at', created_at\n    )\n    ORDER BY created_at DESC\n  ),\n  '[]'::json\n) AS handoffs\nFROM handoffs\nWHERE conversation_id = $1::uuid AND business_id = $2::uuid\nLIMIT 10;",
        "options": {
          "queryReplacement": "={{ String($('When Executed by Another Workflow').first().json.conversation_id || '') }}, {{ String($('When Executed by Another Workflow').first().json.business_id || '') }}"
        },
        "continueOnFail": true
      },
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.7,
      "position": [
        1248,
        -48
      ],
      "id": "get-handoff-node-id",
      "name": "Get Handoff",
      "alwaysOutputData": true,
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "const _h = new Date().toLocaleString('en-US', {timeZone:'America/Jamaica',hour:'2-digit',hour12:false}); const _hr = parseInt(_h, 10); const _greet = _hr >= 12 && _hr < 17 ? 'afternoon' : _hr >= 17 && _hr < 21 ? 'evening' : 'morning'; const trigger = $('When Executed by Another Workflow').first()?.json || {}; const contact = $('Get Contact').first()?.json || {}; const conversation = $('Get Conversation').first()?.json || {}; const messages = $('Get Recent Messages').first()?.json?.messages || []; const facts = $('Retrieve Customer Facts').first()?.json?.facts || []; const summary = $('Get Conversation Summary').first()?.json || { summary: '', message_count: 0, created_by: 'none' }; const knowledge = $('Get Knowledge Chunks').first()?.json?.knowledge || []; const products = $('Get Products').first()?.json?.products || []; const quotes = $('Get Quotes').first()?.json?.quotes || []; const appointments = $('Get Appointments').first()?.json?.appointments || []; const handoffs = $('Get Handoff').first()?.json?.handoffs || []; const business = $('Get Business by Phone').first()?.json || {}; const settings = $('Get Business Settings').first()?.json || {}; return [{ json: { conversation_id: trigger.conversation_id, contact_id: trigger.contact_id, customer: { id: contact.id || trigger.contact_id, customer: contact, phone: contact.phone || trigger.phone, name: contact.display_name || trigger.customer_name }, conversation, recent_messages: messages, customer_facts: facts, conversation_summary: summary, knowledge, products, quotes, appointments, handoff: handoffs.length > 0 ? handoffs[0] : {}, business_name: business.name || settings.setting_value || 'our company', time_greeting: _greet, business_id: business.id || null, business_timezone: business.timezone || 'America/Jamaica', conversationHistory: messages.map(m => `${m.direction === 'in' ? 'Customer' : 'Assistant'}: ${m.text_body}`).join(' | ') || 'No previous messages.', current_message: { message_id: trigger.message_id, text: trigger.message, hasMedia: trigger.hasMedia, timestamp: trigger.timestamp } } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1408,
        176
      ],
      "id": "1ee84b40-7de9-477f-9142-e3d66c8824b3",
      "name": "Build Context Package"
    },
    {
      "alwaysOutputData": true,
      "id": "get-business-settings-node",
      "name": "Get Business Settings",
      "type": "n8n-nodes-base.postgres",
      "credentials": {
        "postgres": {
          "id": "tOjZeRRL1hW2zbUk",
          "name": "Postgres account"
        }
      },
      "typeVersion": 2.7,
      "parameters": {
        "query": "SELECT setting_value FROM settings WHERE setting_key = 'company_name' AND business_id = $1::uuid LIMIT 1;",
        "operation": "executeQuery",
        "options": {
          "continueOnFail": true,
          "queryReplacement": "={{ String($('Get Business by Phone').first().json.id || '') }}"
        }
      },
      "position": [
        1248,
        240
      ]
    }
  ],
  "connections": {
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "Get Business by Phone",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Message Embedding": {
      "main": [
        [
          {
            "node": "Prepare Search Params",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Retrieve Customer Facts": {
      "main": [
        [
          {
            "node": "Get Conversation Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Quotes": {
      "main": [
        [
          {
            "node": "Get Appointments",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Products": {
      "main": [
        [
          {
            "node": "Get Quotes",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Recent Messages": {
      "main": [
        [
          {
            "node": "Retrieve Customer Facts",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Contact": {
      "main": [
        [
          {
            "node": "Get Conversation",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Handoff": {
      "main": [
        [
          {
            "node": "Get Business Settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Business Settings": {
      "main": [
        [
          {
            "node": "Build Context Package",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Conversation Summary": {
      "main": [
        [
          {
            "node": "Generate Message Embedding",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Knowledge Chunks (semantic)": {
      "main": [
        [
          {
            "node": "Get Knowledge Chunks",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Conversation": {
      "main": [
        [
          {
            "node": "Get Recent Messages",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Knowledge Chunks": {
      "main": [
        [
          {
            "node": "Get Products",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Search Params": {
      "main": [
        [
          {
            "node": "Get Knowledge Chunks (semantic)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Appointments": {
      "main": [
        [
          {
            "node": "Get Handoff",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Business by Phone": {
      "main": [
        [
          {
            "node": "Get Contact",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  },
  "id": "p27V1gltwfeIX2yA"
}